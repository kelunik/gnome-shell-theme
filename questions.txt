Theme:
 - Box shadow is real shadow and only possible with border or background!

TeaTime:
// TODO: get colors from current theme!
//cr.setSourceRGB(0, 0, 0);
//cr.rectangle(0, 0, width, height);
//cr.fill();

^ just remove background instead of get current color...
